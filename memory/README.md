# Memory Notes

Use this folder for durable project knowledge that should survive Codex sessions.

Recommended note groups:

- `decisions/` for architecture and policy decisions
- `tasks/` for ongoing work and checkpoints
- `references/` for stable commands, workflows, and external links
- `checkpoints/` for resumable session state
