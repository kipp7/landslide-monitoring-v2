# Runtime Reports

This directory is the public mount point for optional generated runtime and field-verification reports consumed by the API.

Production reports, device evidence, credentials, and site-specific snapshots must remain outside Git. Deployment tooling may mount sanitized report files here at runtime.
