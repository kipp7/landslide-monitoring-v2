
## 2026-06-02（仓库整理与桌面端主线推送方案）

### 背景

- 用户希望整理 `E:\学校\02 项目\99 山体滑坡优化完善` 下的项目文件，后续推送到 Git 管理，同时减少本地空间占用。
- 用户特别强调当前主力版本是桌面端，不能搞错版本。

### 本次做了什么

- 按项目启动规则读取主线说明、月记规则和桌面端交付基线。
- 盘点父目录仓库边界：主线仓库为 `landslide-monitoring-v2-mainline`，旧仓库 `LAMv2_Desk` 保留为历史参考。
- 确认主线仓库远端为 `https://github.com/kipp7/landslide-monitoring-v2.git`，当前 `main` 与 `origin/main` 为 `0 ahead / 0 behind`。
- 盘点主仓库体积：`.tmp` 约 20.74GB，`artifacts` 约 13.81GB，是主要空间占用来源。
- 确认 `key/1.pem` 当前未被 Git 跟踪。
- 新增仓库整理与推送方案文档：`docs/guides/repository-management/repository-organization-and-push-plan.md`。

### 当前结论

- 当前不应直接 `git add . && git commit && git push`，因为工作区变更超过千条，且包含大量不应进入 Git 的临时/构建/数据产物。
- 当前主力版本边界已明确写入文档：默认只围绕 `apps/desk/`、`apps/desk-win/` 和 `artifacts/desk-win/latest*` 管理桌面端交付。
- Web、移动端、旧 `LAMv2_Desk` 不作为默认主线。
- 密钥建议加密后备份入库，不建议明文进入普通 Git 历史。

### 遗留问题

- 尚未执行 Git 提交或推送。
- 尚未生成 `.tmp` / `artifacts` 详细 manifest。
- 尚未处理密钥加密备份包。

### 下一步

- 优先提交仓库管理方案文档。
- 生成大文件 manifest。
- 分批提交桌面端源码、边缘服务、文档和脚本。
- 最后再做本地空间清理。

### CLI 最终输出原文

已经把“桌面端是主力版本”写进仓库整理方案了，文档位置是 `docs/guides/repository-management/repository-organization-and-push-plan.md`。

这份方案明确了：主线只围绕 `apps/desk/`、`apps/desk-win/`、`artifacts/desk-win/latest*` 管理；`apps/web/`、`apps/mobile/`、旧 `LAMv2_Desk/` 不作为默认主线。它也写清了哪些文件进 Git、哪些不能进 Git、密钥怎么加密备份、`.tmp` 和旧交付包怎么归档/清理。

当前还没有提交和推送。下一步我建议先提交这份仓库管理文档，然后生成 `.tmp` / `artifacts` 的大文件 manifest，再开始分批提交桌面端源码和边缘服务代码。

## 2026-06-02（桌面端主线项目整理提交并推送 GitHub）

### 背景

- 用户要求开始整理项目文件，并将项目推送到 GitHub 做统一项目管理。
- 用户强调当前主力版本是桌面端，不能错把 Web/旧桌面仓库当主线。

### 本次做了什么

- 补强 `.gitignore`，新增明文密钥保护规则：`key/`、`*.pem`、`*.key`、`*.p12`、`*.pfx`、`id_rsa*` 等不进普通 Git。
- 新增 `secrets/README.md`，记录密钥备份策略，明确只允许提交加密密钥备份和恢复说明。
- 新增 `docs/guides/repository-management/local-large-files-manifest.md`，登记本地大文件和目录体积，明确 `.tmp`、旧交付包、本地缓存不进普通 Git。
- 修正忽略规则，排除 `apps/desk/dist.backup-*` 这类前端构建备份目录。
- 暂存非忽略项目文件并执行提交前检查。
- 运行桌面端主线构建：`npm run build --workspace landslide-monitor-desk`，结果通过，仅有 Vite chunk size warning。
- 严格密钥扫描通过：未发现私钥块、真实 `.env`、GitHub token、AWS key、JWT 字面量。
- 暂存区和最终跟踪文件大文件检查通过：没有超过 20MB 的 Git 跟踪文件。
- 创建提交并推送到 GitHub。

### 当前结论

- 已推送到 `origin/main`。
- 最新提交：`8e78d98 chore: organize desktop-first project repository`。
- 本地 `main` 与 `origin/main` 已同步。
- `key/1.pem` 仍被 `.gitignore` 忽略，没有进入 Git。
- 当前普通 Git 跟踪文件中没有 `>20MB` 大文件。
- 桌面端主线构建通过。

### 遗留问题

- `.tmp/`、`artifacts/`、`.tools/`、`.bm-state/`、`node_modules/` 等本地大目录仍占空间，尚未执行清理。
- 密钥加密备份包尚未创建，只完成了明文排除和 `secrets/README.md` 策略说明。
- `.git/objects` loose objects 仍约 846MB，后续可在确认无需回滚后执行 `git gc`。

### 下一步

- 先做本地空间清理建议和执行：优先清 `node_modules`、`apps/desk/dist.backup-*`、旧 `artifacts/desk-win/milestones`、旧 `delivery`、`.playwright-mcp` 等可再生成产物。
- 再决定是否对 `key/1.pem` 创建加密备份包。
- 如需减小 `.git` 本地体积，可执行安全的 `git gc`。

### CLI 最终输出原文

项目已经整理并推送好了。

最新提交是 `8e78d98 chore: organize desktop-first project repository`，已推送到 `origin/main`：`https://github.com/kipp7/landslide-monitoring-v2.git`。现在本地 `main` 和 `origin/main` 同步。

这次推送前做了几道保护：`key/1.pem` 仍然被 `.gitignore` 忽略，没有进 Git；暂存区和最终跟踪文件里没有超过 `20MB` 的大文件；严格密钥扫描没有发现私钥块、真实 `.env`、GitHub token、AWS key 或 JWT 字面量；桌面端主线构建 `npm run build --workspace landslide-monitor-desk` 已通过。

我还新增了两份管理文档：`docs/guides/repository-management/repository-organization-and-push-plan.md` 和 `docs/guides/repository-management/local-large-files-manifest.md`，明确桌面端是主力版本，并记录哪些大文件只做 manifest/归档，不进普通 Git。下一步可以开始清理本地空间，优先处理 `node_modules`、`apps/desk/dist.backup-*`、旧 `artifacts/desk-win` 交付包和 `.tmp` 数据归档。

## 2026-06-02（删除区域模型源数据与中间大产物）

### 背景

- 用户决定不再把模型源数据备份到 U 盘，也不需要将模型源数据推送到 Git。
- 目标是直接删除 `.tmp/regional-model-library` 里的源数据和训练中间大产物，释放本地空间。

### 本次做了什么

- 生成并干跑 `scripts/dev/cleanup-regional-model-source-data.ps1`。
- 删除以下目录：
  - `.tmp/regional-model-library/raw`
  - `.tmp/regional-model-library/raw-xlsx-tests`
  - `.tmp/regional-model-library/out`
  - `.tmp/regional-model-library/out-baijiabao-smoke`
  - `.tmp/regional-model-library/out-baijiabao-generic-infer`
  - `.tmp/regional-model-library/out-xlsx-tests`
  - `.tmp/regional-model-library/out-verify-20260421`
  - `.tmp/regional-model-library/test-output`
- 保留源码、文档、轻量 manifest、`artifacts/models`、`artifacts/desk-win/latest*` 和本地 `key/`。
- 将清理决策写入 `docs/guides/repository-management/repository-organization-and-push-plan.md`。
- 提交并推送清理脚本和文档。

### 当前结论

- 最新提交：`ff4ebdb chore: document regional source data cleanup`。
- Git 状态：`main...origin/main`，本地与远端同步。
- 主仓库体积从最初约 `38.96GB` 降至约 `9.28GB`。
- `.tmp` 从约 `20.74GB` 降至约 `0.28GB`。
- `artifacts` 当前约 `5.95GB`，其中 `artifacts/models` 约 `4.66GB`，`artifacts/desk-win` 约 `1.24GB`。

### 遗留问题

- `artifacts/models` 仍是最大剩余目录，约 `4.66GB`；这是最终模型/训练报告资产，后续需要决定是否保留、外部归档或部分 LFS 化。
- `.tools` 约 `0.84GB`、`.bm-state` 约 `0.09GB`，属于本地记忆/工具缓存，是否清理需确认是否还要继续使用记忆系统。
- `.git` 约 `0.88GB`，可后续执行 `git gc` 做本地压缩。

### 下一步

- 如需继续释放空间，优先评估 `artifacts/models` 的备份和清理策略。
- 若暂时不动模型资产，可执行 `git gc` 和工具缓存清理。

### CLI 最终输出原文

已经按你的决定直接删除了模型源数据和训练中间大产物，没有备份到 U 盘，也没有推 Git。

结果：主仓库从最初约 `38.96GB` 降到现在约 `9.28GB`；`.tmp` 从约 `20.74GB` 降到约 `0.28GB`。本地与 GitHub 仍同步，最新提交是 `ff4ebdb chore: document regional source data cleanup`。

保留项没有动：桌面端源码、后端服务、文档、memory、`artifacts/models`、`artifacts/desk-win/latest*`、本地 `key/1.pem` 都还在。

现在剩下的大头主要是 `artifacts/models` 约 `4.66GB`、`.tools` 约 `0.84GB`、`.git` 约 `0.88GB`。如果还要继续释放空间，下一步建议先评估 `artifacts/models` 是否保留最终模型资产，或者先做安全的 `git gc` 和工具缓存清理。
