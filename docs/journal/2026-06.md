
## 2026-06-02（仓库整理与桌面端主线推送方案）

### 背景

- 用户希望整理 `E:\学校\02 项目\99 山体滑坡优化完善` 下的项目文件，后续推送到 Git 管理，同时减少本地空间占用。
- 用户特别强调当前主力版本是桌面端，不能搞错版本。

### 本次做了什么

- 按项目启动规则读取主线说明、月记规则和桌面端交付基线。
- 盘点父目录仓库边界：主线仓库为 `landslide-monitoring-v2-mainline`，旧仓库 `LAMv2_Desk` 保留为历史参考。
- 确认主线仓库远端为 `https://github.com/kipp7/landslide-monitoring-v2.git`，当前 `main` 与 `origin/main` 为 `0 ahead / 0 behind`。
- 盘点主仓库体积：`.tmp` 约 20.74GB，`artifacts` 约 13.81GB，是主要空间占用来源。
- 确认 `key/1.pem` 当前未被 Git 跟踪。
- 新增仓库整理与推送方案文档：`docs/guides/repository-management/repository-organization-and-push-plan.md`。

### 当前结论

- 当前不应直接 `git add . && git commit && git push`，因为工作区变更超过千条，且包含大量不应进入 Git 的临时/构建/数据产物。
- 当前主力版本边界已明确写入文档：默认只围绕 `apps/desk/`、`apps/desk-win/` 和 `artifacts/desk-win/latest*` 管理桌面端交付。
- Web、移动端、旧 `LAMv2_Desk` 不作为默认主线。
- 密钥建议加密后备份入库，不建议明文进入普通 Git 历史。

### 遗留问题

- 尚未执行 Git 提交或推送。
- 尚未生成 `.tmp` / `artifacts` 详细 manifest。
- 尚未处理密钥加密备份包。

### 下一步

- 优先提交仓库管理方案文档。
- 生成大文件 manifest。
- 分批提交桌面端源码、边缘服务、文档和脚本。
- 最后再做本地空间清理。

### CLI 最终输出原文

已经把“桌面端是主力版本”写进仓库整理方案了，文档位置是 `docs/guides/repository-management/repository-organization-and-push-plan.md`。

这份方案明确了：主线只围绕 `apps/desk/`、`apps/desk-win/`、`artifacts/desk-win/latest*` 管理；`apps/web/`、`apps/mobile/`、旧 `LAMv2_Desk/` 不作为默认主线。它也写清了哪些文件进 Git、哪些不能进 Git、密钥怎么加密备份、`.tmp` 和旧交付包怎么归档/清理。

当前还没有提交和推送。下一步我建议先提交这份仓库管理文档，然后生成 `.tmp` / `artifacts` 的大文件 manifest，再开始分批提交桌面端源码和边缘服务代码。
